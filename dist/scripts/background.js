chrome.runtime.onMessage.addListener(async (req, sender, res) => {
    switch(request.type) {
        case "storage":
            if(req.useSync)
            if(req.method === "get") ; else if(req.method === "set") ;
            break;    
    }
});
